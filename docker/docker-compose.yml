services:

  fuseki:
    image: stain/jena-fuseki
    container_name: fuseki-km

    ports:
      - "3030:3030"

    environment:
      ADMIN_PASSWORD: admin

    volumes:
      - ../data/fuseki:/fuseki

    restart: unless-stopped